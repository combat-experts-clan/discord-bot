# Combat Experts Clan Discord Bot

This bot manages clan functions in the Discord server.

# Installation

* Clone the project.
* Run `npm install` to install dependencies.
* Run `npm run bot` or `npm run watch` to start the bot.